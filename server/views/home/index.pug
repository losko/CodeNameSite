extends ../shared/layout
block main-content
    .container.conteiner-my
        h2.text-center.heading-main Code Name Site
        .row
            each item in graphics
                .col-sm-4
                    a(href="/graphics/details/" + item.id)
                        h3.text-center=item.name
                        .index-display-image
                            .inside-conteiner
                                image(src=item.image + '.png').image-display.shadow
                    br
                    small Author : &nbsp;
                    a(href="/users/profile/" + item.author.id)
                        span=item.author.username
                    p
                        small Category : &nbsp;
                        small=item.category
                        br
                        small Views : &nbsp;
                        small=item.views
        .row
            each item in literature
                .col-sm-4
                    a(href="/literature/details/" + item.id)
                        h3=item.name
                        .index-display.lit-a.shadow
                            .inside-conteiner
                    br
                    small Author : &nbsp;
                    a(href="/users/profile/" + item.author.id)
                        span=item.author.username
                    p
                        small Category : &nbsp;
                        small=item.category
                        br
                        small Views : &nbsp;
                        small=item.views
        ul.pagination.text-center
            if(pages)
                if(currentPage !== 0)
                    a(href='/?page=' + (currentPage - 1)).btn.btn
                        span <<<
                -for (var i=0; i<pages; i++)
                    if(currentPage === i)
                        li.active
                            a(href='/?page=' + i).btn.btn
                                span=(i + 1)
                    else
                        li
                            a(href='/?page=' + i).btn.btn
                                span=(i + 1)
                if((currentPage + 1) < pages)
                    a(href='/?page=' + (currentPage + 1)).btn.btn
                        span >>>
    include ../shared/footer
